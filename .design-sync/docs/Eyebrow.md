---
category: Primitives
---
11px mono uppercase label with `0.22em` tracking, in `--muted-foreground`. The standard section kicker above a heading or inside a `Panel`. One step larger and airier than `TinyBrow`.

```jsx
<Eyebrow>Calibration</Eyebrow>
```
