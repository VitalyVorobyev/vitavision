---
category: Primitives
---
Callout block — small muted text behind a 2px `--primary` left rule with a faint primary wash. Use for asides and caveats inside a panel, not for errors (that is what the `--destructive` token is for).

```jsx
<Note>Assumes the target is planar to within 0.1 mm.</Note>
```
