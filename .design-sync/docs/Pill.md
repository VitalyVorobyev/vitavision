---
category: Primitives
---
Rounded-full mono label chip in muted foreground on a translucent background. Use for tags, filter chips, and status labels. Has `inline-flex` with `gap-2`, so an icon child lays out correctly with no extra wrapper.

```jsx
<Pill>ringgrid</Pill>
```
