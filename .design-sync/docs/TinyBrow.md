---
category: Primitives
---
10px mono uppercase label with `0.18em` tracking, in `--muted-foreground`. The smallest label tier — use inside dense chrome such as `MetricCell` rows or floating overlays. Use `Eyebrow` for section headers, which is one step larger.
